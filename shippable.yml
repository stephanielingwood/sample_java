language: java

jdk:
   - openjdk7

build:
   ci:
      - apt-get update
      - env
      - which mvn
      - ls -al /usr/bin/mvn
      - mvn --version
      - apt-get install -y maven
   on_success:
      - mvn clean package
      - mvn clean cobertura:cobertura
      - mvn test
