language: java

jdk:
   - openjdk7

build:
   advancedReporting: true
   ci:
      - apt-get update
      - apt-get install tree
      - mvn --version
      - mkdir -p shippable/codecoverage
   on_success:
      - mvn clean package
      - mvn test
      - tree
      - cp shippable/codecoverage/target/site/jacoco/jacoco.xml shippable/codecoverage/jacoco.xml
      - cat shippable/codecoverage/jacoco.xml
